plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
    id('shared-dependencies')
}

android {
    namespace 'com.segunfrancis.author'
}

dependencies {
    implementation(project(":data:local"))
    implementation(project(":shared:theme"))
}
